import 'package:flutter/material.dart';

class ComprarPassagemAereaView extends StatelessWidget {
  const ComprarPassagemAereaView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Center(
        child: Text('tela de compra'),
      ),
    );
  }
}
